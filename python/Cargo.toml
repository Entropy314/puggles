[package]
name = "rustypus-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "rustypus"
crate-type = ["cdylib"]

[features]
gpu = ["rustypus/gpu"]

[dependencies]
rustypus = { path = ".." }
pyo3 = { version = "0.23", features = ["extension-module"] }
rayon = "1.10.0"
