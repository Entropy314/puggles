[package]
name = "rustypus"
version = "0.1.0"
edition = "2021"

[features]
gpu = ["dep:wgpu", "dep:pollster"]

[dependencies]
rand = "0.8.5"
rand_distr = "0.4.3"
rayon = "1.10.0"
wgpu = { version = "22", optional = true }
pollster = { version = "0.3", optional = true }
